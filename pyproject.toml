[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "weather-aware-travel-itinerary-optimization"
version = "0.1.0"
description = "Weather-aware multi-day itinerary optimization with nature-aware planning extensions."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.111,<1",
  "folium>=0.16",
  "geopy>=2.4",
  "gurobipy>=11.0",
  "jupyter>=1.0",
  "matplotlib>=3.7",
  "numpy>=1.24",
  "openai>=2.45,<3",
  "openrouteservice>=2.3",
  "ortools>=9.10",
  "pandas>=2.0",
  "pydantic>=2.7,<3",
  "requests>=2.31",
  "scikit-learn>=1.3",
  "xgboost>=2.0",
  "uvicorn>=0.30,<1",
]

[project.optional-dependencies]
product-test = [
  "httpx>=0.27,<1",
  "playwright>=1.45,<2",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 120
target-version = "py312"
exclude = [
  ".git",
  ".venv",
  "__pycache__",
  "cache",
  "htmlcov",
  "notebook",
  "notebook/__pycache__",
  "results",
]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"tests/*.py" = ["E402"]
"src/itinerary_system/experiment_runner.py" = ["B023"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-p no:cacheprovider"

[tool.coverage.run]
source = ["src/itinerary_system"]
omit = [
  "*/__init__.py",
  "*/_legacy.py",
]

[tool.coverage.report]
show_missing = true
skip_empty = true
