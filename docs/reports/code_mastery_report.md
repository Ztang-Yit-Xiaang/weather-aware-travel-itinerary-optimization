# Code Mastery Documentation Report

**Date:** 2026-07-29  
**Phase:** Phase B — repository-specific learning documentation  
**Result:** complete documentation package created from current source/tests

## Delivered

`docs/code_mastery/` now contains:

- a guided reading path;
- current package/repository map;
- authoritative CLI/package entry points;
- immutable pipeline/artifact flow;
- parent/child/ownership/repository/diff contracts;
- route matrix and road-validation rules;
- progressive, heuristic, and exact repair behavior;
- independent evaluator and metric ownership;
- explanation/failure evidence;
- modular dashboard and Folium renderer ownership;
- interaction probe/permission/continuation boundaries;
- exact PowerShell testing layers and current test count;
- a debugging playbook;
- legacy/current compatibility boundaries;
- current gate map; and
- repository-specific glossary.

The `diagrams/` directory contains Mermaid diagrams for all requested flows:
package entry/pipeline, immutable artifacts, four-method benchmark,
parent-child repair, exact refusal/D1, independent evaluation, explanation
grounding, dashboard loading, Folium modules, modular dashboard modules,
interaction probe versus authorized repair, and test hierarchy.

## Evidence Basis

The documentation was traced from current package symbols, their callers,
tests, current manifests/plans, and immutable v14 artifact readback. It does not
reuse the July 6 claim that evaluator, explanation, benchmark, pipeline, or
interaction packages are missing.

## Test Count

Verified collection command:

```powershell
python -m pytest --collect-only -q
```

Result:

```text
315 tests collected in 1.60s
```

The guide explicitly treats 315 as a current observation, not a permanent
acceptance constant.

## Beginner Support

Each difficult chapter includes `Beginner note / 初学者提示`, concrete
repository examples, read-next routing, and/or short quiz questions. The
package intentionally avoids generic Python tutorials.

## Current Truth Preserved

- E1/E2/E3.0/E3.M/E3.2/E3.C1–C4 are verified.
- E3.1/E3.3/E4 remain blocked.
- E3.UX0–E3.UX4 are verified through the additive v6 product artifact.
- E3.UX5 remains deferred.
- The interaction scaffold exists, but E5 remains deferred and default mode is
  disabled.
- v14 remains immutable diagnostic evidence and not a complete four-method
  comparison.

## Remaining Work

D1/E3.1 is the next technical blocker. Code-mastery chapters must be refreshed
when exact-baseline strategy or later gate evidence changes.
